{% extends 'layouts/base.njk' %}

{% block content %}
  <main>
    {% include "header.njk" %}
    {% if title %}
      <h1>{{ title }}</h1>
    {% endif %}

    {% if showPosts %}
      {% set cmsPosts = cms %}
      {% include "posts.njk" %}
    {% endif %}

    {{ content | safe }}
  </main>
{% endblock %}
